/* variables */
nullController = thisComp.layer("▣ void-global-controls"); // pickwhip to null
delayedProperty = ; // pickwhip to property and change name to 'index - 1'
timeDelay = nullController.effect("delay")("Slider")*thisComp.frameDuration;

/* delay */
delayedProperty.valueAtTime(time-timeDelay);