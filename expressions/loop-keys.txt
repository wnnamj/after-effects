if (numKeys >1 && time > key(numKeys).time) {
  
  /* variables */
  t1 = key(1).time;
  t2 = key(numKeys).time;
  span = t2 - t1;
  delta = time - t2;
  t = delta%span;
  
  /* loop path keyframes */
  valueAtTime(t1 + t)
}

else {
  value;
}